# =====================================================
# MESS MANAGER PRO - Environment Variables
# =====================================================

# Application URL (SECURITY: Used for OAuth redirects)
VITE_APP_URL=https://yourdomain.com

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# =====================================================
# CLOUDINARY CONFIGURATION (for image uploads)
# =====================================================
# Cloudinary provides direct client-side uploads with unsigned presets
# These values are SAFE to expose in client-side code
# 
# Setup instructions:
# 1. Create a free Cloudinary account at https://cloudinary.com
# 2. Go to Settings > Upload > Upload presets
# 3. Add an upload preset with:
#    - Signing Mode: Unsigned
#    - Folder: mess-manager
#    - Allowed formats: jpg, png, pdf
# 4. Copy the cloud name and preset name below
VITE_CLOUDINARY_CLOUD_NAME=dpgm0tzyn
VITE_CLOUDINARY_UPLOAD_PRESET=Mess-Flow
# =====================================================
# EDGE FUNCTION SECRETS (Set via Supabase CLI)
# =====================================================
# Run: supabase secrets set VARIABLE_NAME="value"

# ALLOWED_ORIGINS - Comma-separated list of allowed origins for CORS
# Example: ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"

# ENVIRONMENT - Set to "development" for verbose logging
# Example: ENVIRONMENT="production"

# =====================================================
# SECURITY NOTES
# =====================================================
# 1. NEVER commit this file with real values to version control
# 2. Use different values for development and production
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Use strong, unique values for all secrets
# 5. Limit ALLOWED_ORIGINS to your actual domains only
# 6. Cloudinary variables are safe to expose (unsigned upload preset)
